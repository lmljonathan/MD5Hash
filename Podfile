# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'

target 'MD5Hash' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  # Pods for MD5Hash
  pod 'DMSwipeCards'
  pod 'Kingfisher'
  pod 'OAuthSwift'

  target 'MD5HashTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'MD5HashUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
